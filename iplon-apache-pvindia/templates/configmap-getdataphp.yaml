apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configmap_getdataphp.name }}
  namespace: {{ .Values.namespace  }}

data:
  
{{- range $path, $_ := (.Files.Glob "configmap-getdataphp/*") }}
  {{ trimPrefix "configmap-getdataphp/" $path }}: |+
    {{- tpl ($.Files.Get $path) $ | nindent 4 }}
{{ end -}}